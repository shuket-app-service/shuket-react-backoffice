import React from "react";
import { Box, LinearProgress } from "@mui/material";

const ProgressLoad = () => {
   return (
    <Box sx={{ width: '100%' }}>
      <LinearProgress />
    </Box>
   );
};

export default ProgressLoad;
