import { Card, CardContent } from "@mui/material";
import React from "react";

const AppDisplayAdd = () => {
   return (
      <Card>
         <CardContent>add</CardContent>
      </Card>
   );
};

export default AppDisplayAdd;
